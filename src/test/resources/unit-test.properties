# Properties for API REST Example Tests
user=<user>
password=<password>
baseUri=<url>

# this should be replaced after initial insert is done
# for a metadata, this can be used for querying metadata
# and uploaded and downloading datasets
# Prevents having ids within test source
metadataId=<metadata id>
